import * as React from 'react';
//
import { Story } from '../../types';

export interface CommentsProps {
  story: Story;
}

const Comments = ({ }: CommentsProps) => <div>Comments</div>;

export default Comments;